{
  "name": "@olympus-dev/protocol",
  "version": "1.0.1",
  "type": "module",
  "description": "Shared protocol types and WebSocket message schemas for Olympus",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "vitest": "^3.0.0"
  }
}
